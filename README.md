# AIHackathon202604

This repository contains a Node.js web service and an Angular client that run together with Docker Compose.

## Run with Docker Compose

From the repository root:

```bash
docker compose up --build
```

- Angular client: http://localhost:4200
- Node.js API: http://localhost:3000/api/message
